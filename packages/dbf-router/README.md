# DBF Router

Minimal, HTML-first client-side router designed to pair nicely with **DBF Core**.

## Goals

- Keep things small and explicit
- Work with `dbf-core` components or plain DOM
- Prefer HTML usage (`<a dbf-link>` etc.) over heavy config

## Status

Early experiment. API is intentionally tiny and may evolve.


