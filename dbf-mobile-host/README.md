# DBF Mobile Host (MVP)

This directory contains native host scaffolds for iOS and Android. The host app
loads a local `webapp/` bundle into a WebView and exposes the DBF bridge.

Structure:

- `ios/` Swift WKWebView scaffold
- `android/` Kotlin WebView scaffold
- `webapp/` build output placeholder

